const config = {
  tile_id: 0,
  tests: {
    exit: {
      position: true,
      velocity: true,
      size: true,
      shape: true,
      render: true,
    },
  },
  debug: {
    showMasses: false,
    showTimer: true,
    showMousePosition: true,
  },
};

export default config;
